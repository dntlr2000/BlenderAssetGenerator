# V0.8 portable smoke evidence — 2026-08-11

이 snapshot은 최종 V0.8 smoke의 standard proxy, background preview/package, fast QA와 package
continuation workflow state를 보존한다. FBX package manifest, clean-import roundtrip, canonical
seven-pass render manifest와 Visual QA report를 함께 포함한다.

전체 workflow artifact tree, renders, source `.blend`, package payload는 포함하지 않는다. 현재
설치가 같은 결과를 낸다는 주장은 V0.8 gate 재실행으로 확인해야 한다.

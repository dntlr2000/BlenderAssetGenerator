# Codex ImageGen material-loop orchestration

Use this prompt only for the explicit `autonomous_static_prop_v2_codex_imagegen`
`disabled_experimental` profile. Preserve project `0.9.0`, canonical SceneSpec `0.2.0`, existing
ImageGen core `0.1.0`, MaterialAuthoring `0.2.1`, and every immutable historical artifact.

1. Load the exact current AQ v2 root authorization, plan, profile, budget, predecessor state,
   canonical SceneSpec, geometry receipt, build fingerprint, UV identity, and MaterialPlan baseline.
   Stop on missing, stale, linked, escaped, or mismatched evidence.
2. Use only the current Codex task's built-in ImageGen capability. Do not add API credentials,
   SDK/HTTP providers, a daemon, or claims that the repository creates or resumes Codex tasks.
3. Preserve a native PNG as immutable original evidence. Normalize only through a canonical,
   requested-operation-bound derivative and carry the adoption, normalization, and
   `CodexImageNativeCorePreparationReceipt` closure into core selection and the material bridge.
4. Require current-task semantic review for every candidate with `human_reviewed=false`. For multiple
   candidates, require exact ranking evidence and use file hard gate, deterministic quality,
   semantic outcome, material-role suitability, repair cost, then stable candidate ID. Missing or
   unresolved evidence yields `review_required`.
5. Treat ImageGen pixels only as `base_color`, `decal_rgb`, `emission`, or `opacity_source` candidates.
   Derive normal, roughness, metallic, height, displacement, AO, and tangent evidence locally under
   the existing bounded contracts. Compose exact signage text locally from hash-bound font evidence.
6. Preserve the V0.5 staging receipt's `not_run` meaning. Use `exact_adoption` only with a separate
   actual-Blender shadow-preflight receipt for the exact candidate bytes; otherwise use bounded
   controller-authored completion or wait.
7. Publish the exact bridge/controller input closure and use the existing ControllerExecutor.
   Permit only `material_plan.json`, `material_graph.json`, and `completion.json`; never hand-write a
   ControllerResult or grant canonical/destination write authority.
8. Promote only through the existing host material phase service. Require compare-and-swap,
   MaterialGraph compilation, Blender rebuild/inspection/validation, rollback or exact pre-write
   failure evidence, actual `MaterialPhaseReceiptV2`, fixed neutral preview, and companion receipt.
9. Resume base AQ through its existing transition service. `material_promoted` is not IQ pass.
   Advance IQ by one exact action; only a passed terminal and source freeze may become
   `quality_approved`.
10. Stop delivery at `review_only` or `waiting_for_v07_approval` unless the user supplies the exact
    specialized approval. Never synthesize OptimizationApproval, PolicyAuthorization, human review,
    package acceptance, destination write, or runtime parity.
11. Recovery must adopt the same immutable request/result/intent/receipt without duplicate budget.
    Never repair history, infer success from exception text, or reuse evidence after SceneSpec, UV,
    source, profile, authorization, or canonical-baseline drift.
12. Report current/stale/unverified, selected candidate, native/normalization/semantic evidence,
    controller lifecycle, promotion receipt and `MaterialPhaseReceiptV2`, base next action, IQ and
    delivery state, remaining budget, and latest exact failure.


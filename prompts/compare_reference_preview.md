# V0.6 direct visual QA prompt

Compare the immutable reference evidence against fixed-camera render passes. Prioritize silhouette, normalized bounding boxes, observed semantic regions, landmark positions, negative space, depth order, and measured residuals. Use beauty/color judgments only after shader-independent geometry checks. Localize findings to stable IDs and do not propose changes to locked camera or unrelated objects.


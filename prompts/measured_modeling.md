# Measured modeling workflow

Use registered orthographic views, blueprints, and scale anchors. Create or update `constraints/constraints.json` with semantic targets and tolerances. Build and inspect the SceneSpec-derived Blender scene, then evaluate constraints. Do not claim measured accuracy from schema validation alone. Turn residuals into small ID-addressed revisions and preserve unrelated geometry.
